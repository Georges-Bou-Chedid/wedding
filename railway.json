{
    "build": {
      "builder": "php",
      "startCommand": "composer install && php artisan migrate && php artisan serve --host=0.0.0.0 --port=$PORT"
    },
    "deploy": {
      "builds": ["php"]
    }
  }
  